// API configuration for different environments
// All environments now use the production Render URL to ensure consistency
const RENDER_URL = 'https://ira-project-ep8l.onrender.com';

export const API_BASE = RENDER_URL;
